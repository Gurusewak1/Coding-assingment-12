export interface LinkProps {
  href: string;
  label: string; // 'label' should be included here
  color?: string;
  bold?: boolean;
  italic?: boolean;
  visible?: boolean;
  disabled?: boolean;
  backgroundColor?: string;
}
